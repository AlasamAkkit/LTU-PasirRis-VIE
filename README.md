# LTU-PasirRis-VIE

## How to Run

This project starts from `game.bootstrap.Main`.

In VS Code, run the `Run: Main` configuration.

If you want to run it from the terminal, use `./gradlew run` on macOS or Linux, or `gradlew.bat run` on Windows.